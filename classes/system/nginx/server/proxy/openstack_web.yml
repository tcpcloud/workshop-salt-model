parameters:
  nginx:
    server:
      enabled: true
      site:
        nginx_redirect_horizon:
          enabled: true
          type: nginx_redirect
          name: horizon
          host:
            name: ${_param:cluster_public_host}
            port: 80