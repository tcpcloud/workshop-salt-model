classes:
- service.horizon.server.single
- system.openstack.common.workshop
parameters:
  horizon:
    server:
      theme: tcp
      bind:
        # Horizon is proxied by Nginx
        host: 127.0.0.1
        port: 8078
