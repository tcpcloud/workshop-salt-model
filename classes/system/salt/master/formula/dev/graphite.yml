parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            apache:
              source: git
              address: '${_param:salt_master_environment_repository}/apache-formula.git'
              revision: ${_param:salt_master_environment_revision}
            graphite:
              source: git
              address: '${_param:salt_master_environment_repository}/graphite-formula.git'
              revision: ${_param:salt_master_environment_revision}
            postgresql:
              source: git
              address: '${_param:salt_master_environment_repository}/postgresql-formula.git'
              revision: ${_param:salt_master_environment_revision}
